export interface Pokemon {
    name: string;
    url: string; // tentar usar a api que mostra a imagem 
}

export interface WeatherData {
    city: string;
    temperature: number;
    isRaining: boolean;
}